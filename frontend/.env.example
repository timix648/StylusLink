# StylusLink Frontend Environment Variables
# Copy this file to .env.local and fill in your values

# Backend API URL (use ngrok URL when backend is running locally)
# For local development: http://localhost:4000/api
# For production with ngrok: https://your-subdomain.ngrok-free.dev/api
NEXT_PUBLIC_GATEKEEPER_URL=https://ranchlike-mark-nonbookishly.ngrok-free.dev/api

# WalletConnect Project ID (optional but recommended)
# Get a free one at: https://cloud.walletconnect.com
# Without this, WalletConnect mobile wallets won't work (MetaMask browser still works)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your-project-id-here
